# 系统流程说明

本文档详细描述 Bend Platform 系统的业务流程和数据流向。

---

## 目录

1. [Agent 注册流程](#agent-注册流程)
2. [任务下发流程](#任务下发流程)
3. [自动化控制流程](#自动化控制流程)
4. [商户入驻流程](#商户入驻流程)
5. [心跳保活机制](#心跳保活机制)
6. [系统监控](#系统监控)

---

## Agent 注册流程

### 流程图

```
┌─────────┐         ┌──────────────┐         ┌─────────┐
│  商户   │         │   平台后端   │         │  Agent │
└────┬────┘         └──────┬───────┘         └────┬────┘
     │                     │                       │
     │ 1.生成注册码        │                       │
     │────────────────────>│                       │
     │                     │                       │
     │ 2.发放注册码        │                       │
     │<────────────────────│                       │
     │                     │                       │
     │                     │   3.首次运行Agent     │
     │                     │<──────────────────────│
     │                     │                       │
     │                     │ 4.API: /api/agents/register
     │                     │<──────────────────────│
     │                     │                       │
     │                     │ 5.创建AgentInstance   │
     │                     │ 6.更新注册码状态      │
     │                     │                       │
     │                     │ 7.返回agentId/secret │
     │                     │──────────────────────>│
     │                     │                       │
     │ 8.Agent上线显示      │                       │
     │<────────────────────│                       │
     │                     │                       │
```

### 重新上线流程

Agent因网络断开等原因离线后，重新上线的流程：

```
Agent              平台后端
  │                   │
  │ 1.网络恢复重连     │
  │                   │
  │ 2.API: /api/agents/register
  │   (使用原agentId+secret)
  │<──────────────────>│
  │                   │
  │ 3.平台识别已存在   │
  │ 4.更新状态为online │
  │                   │
  │ 5.返回确认        │
  │<──────────────────>│
```

---

## 任务下发流程

### 流程图

```
┌─────────┐     ┌──────────────┐     ┌─────────────┐     ┌─────────┐
│  管理员  │     │   平台后端   │     │  WebSocket  │     │  Agent  │
└────┬────┘     └──────┬───────┘     └──────┬──────┘     └────┬────┘
     │                 │                     │                │
     │ 1.创建自动化任务 │                     │                │
     │────────────────>│                     │                │
     │                 │                     │                │
     │                 │ 2.保存任务到数据库   │                │
     │                 │────────────────────>│                │
     │                 │                     │                │
     │                 │ 3.WebSocket推送任务  │                │
     │                 │────────────────────>│                │
     │                 │                     │                │
     │                 │                     │ 4.接收并解析任务 │
     │                 │                     │                │
     │                 │                     │ 5.执行任务      │
     │                 │                     │                │
     │                 │ 6.任务结果回调       │                │
     │                 │<────────────────────│                │
     │                 │                     │                │
     │ 7.更新任务状态   │                     │                │
     │<────────────────│                     │                │
     │                 │                     │                │
```

### 任务状态流转

```
                    ┌─────────┐
                    │ pending │
                    └────┬────┘
                         │
            ┌────────────┼────────────┐
            │            │            │
            ▼            ▼            ▼
      ┌──────────┐  ┌──────────┐  ┌──────────┐
      │ running  │  │cancelled │  │  failed  │
      └────┬─────┘  └──────────┘  └──────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌────────┐   ┌────────┐
│completed│   │  failed│
└────────┘   └────────┘
```

---

## 自动化控制流程

### 架构图

```
流媒体账号列表 ──> 创建自动化任务 ──> 分配给Agent ──> 执行自动化
                                         │
                                         ▼
                                  WebSocket下发
                                         │
                                         ▼
                       ┌─────────────────┴────────────────┐
                       │                                   │
                   Xbox SmartGlass                      输入模拟
                       │                                   │
                       ▼                                   ▼
                 连接Xbox主机                         模板匹配
                       │                                   │
                       ▼                                   ▼
                 控制云游戏                           图像识别
```

### 任务类型

| 类型 | 说明 | 适用场景 |
|------|------|----------|
| template_match | 模板匹配 | 界面按钮点击 |
| input_sequence | 输入序列 | 复杂操作序列 |
| scene_detection | 场景检测 | 游戏状态识别 |
| account_switch | 账号切换 | 多账号管理 |
| stream_control | 串流控制 | 远程控制 |
| custom | 自定义 | 扩展任务 |

---

## 商户入驻流程

### 流程图

```
┌─────────┐     ┌──────────────┐     ┌──────────────┐
│  新商户  │     │   平台管理员  │     │   平台后端   │
└────┬────┘     └──────┬───────┘     └──────┬───────┘
     │                 │                     │
     │ 1.注册商户账号   │                     │
     │────────────────>│                     │
     │                 │                     │
     │                 │ 2.审核商户           │
     │                 │────────────────────>│
     │                 │                     │
     │                 │ 3.创建商户记录       │
     │                 │<────────────────────│
     │                 │                     │
     │ 4.获取商户ID    │                     │
     │<────────────────│                     │
     │                 │                     │
     │ 5.配置VIP        │                     │
     │────────────────>│                     │
     │                 │                     │
     │ 6.生成注册码     │                     │
     │<────────────────│                     │
     │                 │                     │
     │ 7.分发注册码给商户下的Agent            │
     │────────────────>│                     │
     │                 │                     │
```

---

## 心跳保活机制

### 心跳配置

```yaml
agent:
  heartbeat_interval: 30  # 心跳间隔（秒）
```

### 心跳内容

Agent 运行时定期发送心跳，包含：
- Agent 当前状态
- 正在执行的任务ID
- 当前流媒体账号ID
- 运行中的任务数
- Xbox 会话数

### 离线检测

```
在线状态判断:
  if (now - lastHeartbeat > 5分钟):
      状态设为 "offline"
      触发离线告警

Agent重新上线:
  if (status == "offline" AND agentId存在):
      status = "online"
      清除离线告警
```

---

## 系统监控

### 监控指标

| 指标类型 | 指标名称 | 说明 |
|---------|---------|------|
| JVM | memory_used | JVM已使用内存 |
| JVM | memory_usage_percent | JVM内存使用率 |
| 系统 | cpu_usage_percent | 系统CPU使用率 |
| 系统 | memory_usage_percent | 系统内存使用率 |
| 业务 | agent_online_count | 在线Agent数量 |
| 业务 | task_running_count | 运行中任务数量 |
| 业务 | task_success_rate | 任务成功率 |

### 告警类型

| 告警类型 | 级别 | 说明 |
|---------|------|------|
| AGENT_OFFLINE | HIGH | Agent离线超过5分钟 |
| TASK_FAILED | MEDIUM | 任务连续失败3次 |
| HIGH_CPU | MEDIUM | CPU使用率超过90% |
| HIGH_MEMORY | MEDIUM | 内存使用率超过90% |
| XBOX_CONNECTION_FAILED | HIGH | Xbox连接失败 |

### 监控API

```bash
# 获取JVM信息
GET /api/monitoring/jvm

# 获取系统信息
GET /api/monitoring/system

# 获取业务统计
GET /api/monitoring/stats

# 获取告警列表
GET /api/monitoring/alerts

# 确认告警
POST /api/monitoring/alerts/{id}/acknowledge

# 解决告警
POST /api/monitoring/alerts/{id}/resolve
```

---

## 数据流向总图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           前端 (bend-platform-web)                        │
│   Vue 3 + Element Plus + Pinia + Axios (带请求重试/取消)                 │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │ HTTP / WebSocket
┌────────────────────────────────▼────────────────────────────────────────┐
│                           后端 (bend-platform)                            │
│   Spring Boot 3.2 + MyBatis-Plus 3.5.5 + Redis                          │
│                                                                          │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│   │ Controller│  │ Service  │  │  Mapper  │  │   WebSocket│             │
│   └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘              │
│        │              │             │             │                    │
│        └──────────────┴─────────────┴─────────────┘                    │
│                                │                                        │
│                    ┌───────────┴───────────┐                           │
│                    │                       │                            │
│              ┌─────▼─────┐          ┌─────▼─────┐                      │
│              │   MySQL    │          │   Redis   │                      │
│              │  8.0+      │          │  6.0+     │                      │
│              └───────────┘          └───────────┘                      │
└─────────────────────────────────────────────────────────────────────────┘
                                 │ WebSocket
┌────────────────────────────────▼────────────────────────────────────────┐
│                           Agent (bend-agent)                             │
│   Python 3 + asyncio + SmartGlass + OpenCV                              │
│                                                                          │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐          │
│   │ CentralManager│  │ TaskExecutor │  │ XboxSessionManager   │          │
│   │   核心管理   │  │  任务执行   │  │   会话管理(100+)    │          │
│   └──────────────┘  └──────────────┘  └──────────────────────┘          │
│                                                                          │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐          │
│   │ StreamCtrl   │  │TemplateMath │  │   VideoCapture      │          │
│   │ SmartGlass   │  │  模板匹配   │  │    帧捕获           │          │
│   └──────────────┘  └──────────────┘  └──────────────────────┘          │
│                                                                          │
│                         Windows PC                                        │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 相关文档

- [用户操作指南](user-guide.md)
- [开发说明](development.md)
- [API文档](../bend-platform-api.json)
